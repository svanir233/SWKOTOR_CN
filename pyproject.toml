[project]
name = "swkotor-cn-tools"
version = "0.1.0"
description = "Build tools for the SWKOTOR_CN localization project."
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"
dependencies = [
    "pillow>=11.0",
]

[tool.uv]
package = false

[tool.ruff]
line-length = 120
